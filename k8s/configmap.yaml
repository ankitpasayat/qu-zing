apiVersion: v1
kind: ConfigMap
metadata:
  name: qu-zing-config
  namespace: qu-zing
data:
  NODE_ENV: "production"
  PORT: "3000"
  CLIENT_URL: "http://client-service"
  # Discord Client ID is managed via secrets (DISCORD_CLIENT_ID)
